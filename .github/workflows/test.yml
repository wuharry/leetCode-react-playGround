name: Test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v3
        with:
          bun-version: "1.3.8"
          cache: "bun"
      - run: bun install
      - run: bun run check # type-check + lint + test
